# Sharing & Collaboration

1. Share button generates a shareable read-only link with a token
2. Public view (TripPublicView) shows itinerary without edit capabilities
3. Public view doesn't require login
4. Trip members with "edit" role can edit activities, delete trip
5. Trip members list shows in the itinerary header area
